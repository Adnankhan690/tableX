meta {
  name: public
  seq: 3
}

auth {
  mode: inherit
}

docs {
  Unauthenticated, rate limited. The diner's entry points plus the restaurant directory.

  Run `scan-table` first: it is what mints a guest token, and almost every other folder depends
  on one.
}
