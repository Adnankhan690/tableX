meta {
  name: admin-settings
  seq: 10
}

auth {
  mode: inherit
}

docs {
  Restaurant configuration. Rates are integer basis points (500 = 5.00%), which is
  what keeps every money computation integral (docs/DECISIONS.md D7).
}
