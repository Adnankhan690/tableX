meta {
  name: admin-tables
  seq: 8
}

auth {
  mode: inherit
}

docs {
  Tables and their QR codes. `rotate-qr` is destructive to a physical object: the
  printed sticker stops working immediately and must be reprinted.
}
