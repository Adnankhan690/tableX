meta {
  name: admin-menu
  seq: 7
}

auth {
  mode: inherit
}

docs {
  Menu management. Note that `set-availability` is open to every staff role while the
  rest is owner/manager -- marking a dish sold out is a floor action taken mid-service, and routing
  it through a manager means diners keep ordering something the kitchen ran out of.
}
