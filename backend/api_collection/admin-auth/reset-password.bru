meta {
  name: reset-password
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/admin/v1/auth/reset-password
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "owner@spicegarden.test",
    "code": "123456",
    "new_password": "password123_new"
  }
}

tests {
  test("responds 400 when invalid/expired", function () {
    expect(res.status).to.equal(400)
  })
}
