meta {
  name: forgot-password
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/admin/v1/auth/forgot-password
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "owner@spicegarden.test"
  }
}

tests {
  test("responds 200", function () {
    expect(res.status).to.equal(200)
  })
}
